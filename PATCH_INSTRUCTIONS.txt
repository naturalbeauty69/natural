NATURAL BEAUTY — ACADEMY FILTER + PERFORMANCE PATCH

Replaces only:
- app/academy/resources/page.tsx
- components/academy/AcademyResourceList.tsx
- components/Header.tsx
- components/nav/FloatingActions.tsx
- .gitignore

Academy:
- Adds fast client-side search.
- Adds filters: All, Notice, Syllabus, Files, Images, Links.
- Adds course filter using the course relation already available through the courses foreign key.
- Does NOT change existing RLS/access behavior.

Performance:
- Header scroll state updates are throttled with requestAnimationFrame and only update React when the state actually changes.
- Removes expensive backdrop blur from the sticky header.
- Removes Framer Motion JS from floating action buttons and the continuous WhatsApp ping animation.
- Adds generated/cache folders to .gitignore.

No SQL is included and no Supabase data is modified by this patch.

Recommended verification after deployment:
1. Open /academy/resources.
2. Test search/type/course filters.
3. Test public notice/syllabus as visitor.
4. Test an authenticated/private resource as its authorized user.
5. Check Cloudflare Worker CPU logs for /academy/resources and normal navigation.
