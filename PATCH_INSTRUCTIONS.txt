FINAL MIDDLEWARE FIX

Replace ONLY:
middleware.ts

This file restores the known-good middleware implementation:
- updateSession(request) returns { response, user, supabase }
- middleware returns response
- no undefined `response` variable
- no dashboard SQL/CPU changes

DO NOT run Supabase SQL for this patch.

After replacing the file:
git add middleware.ts
git commit -m "Fix middleware response build error"
git push

Then wait for Cloudflare to build the new commit.

IMPORTANT:
Make sure GitHub's middleware.ts is the same as this patch before pushing. Cloudflare must build the commit containing this file.
