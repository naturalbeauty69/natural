ADMIN DASHBOARD CPU FIX - FINAL

Replace ONLY:
app/admin/dashboard/page.tsx

Do not modify:
- Supabase SQL
- middleware.ts
- app/admin/layout.tsx
- other website files

The dashboard statistics now load in the browser instead of during the Cloudflare Worker request.
After replacing the file:
1. commit
2. push to GitHub
3. let Cloudflare build
4. test /admin/dashboard

Do not run any SQL for this patch.
