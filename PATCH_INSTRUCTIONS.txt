ACADEMY ADMIN LOGIN FIX

Replace only:
  middleware.ts

Reason:
The Admin layout expects x-admin-role, x-admin-user-email, and x-admin-active request headers, but the previous middleware never set them. After successful authentication, /admin/dashboard reached the layout as guest/inactive and redirected back to /login.

No Supabase SQL changes are required.
After replacing middleware.ts, commit and deploy. Verify admin login before making any other changes.
