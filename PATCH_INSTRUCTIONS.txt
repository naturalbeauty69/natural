PUBLIC ACADEMY RESOURCES FIX
===============================

Replace only:
app/academy/resources/page.tsx

Do NOT replace middleware.ts, Supabase SQL, products files, or other Academy files.

What this fixes:
- /academy/resources no longer forces every visitor to log in.
- Supabase RLS remains responsible for resource-level access.
- access_level=public rows are visible anonymously.
- students/approved/staff rows remain restricted by the existing RLS policy.
- Existing /api/academy/resources/[id] handling remains unchanged.

After replacing the file:
1. Commit and push to GitHub.
2. Let Cloudflare deploy.
3. Open https://naturalbeauty.com.np/academy/resources in a private/incognito browser window.
4. Confirm the two public resources appear without login.
5. Click Open / Download and confirm the file opens.

No SQL needs to be run for this patch.
