Admin Dashboard CPU-limit fix

Replace ONLY these files in the existing project:
- app/admin/dashboard/page.tsx
- app/admin/layout.tsx
- middleware.ts

What this fixes:
1. Moves the 8 dashboard count queries from the Cloudflare Worker server render to the authenticated browser.
2. Avoids repeating Supabase auth/profile queries in app/admin/layout.tsx after middleware has already verified the session.
3. Keeps authorization in middleware.
4. Does not modify Supabase schema/data.

Do NOT run any SQL for this patch.
Do NOT replace the rest of the website.

After replacing the files:
npm install
npm run build

Then commit and push to GitHub and test /admin/dashboard.
